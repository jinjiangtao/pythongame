帮我用 Python 和 CustomTkinter 开发一个图片批量压缩工具，界面友好，功能精简实用。代码必须拆分到多个文件，方便维护。

一、文件结构要求

```
image_compressor/
├── main.py           # 程序入口，启动应用
├── app.py            # 主窗口类，负责界面布局和事件绑定
├── compressor.py     # 压缩逻辑类，处理图片压缩算法
├── file_selector.py  # 文件选择相关功能（多选图片、选择输出文件夹）
├── task_manager.py   # 任务管理（列表维护、进度更新、异常处理）
└── utils.py          # 辅助函数（格式化文件大小、弹窗提示等）
```

每个文件职责单一，通过 import 互相调用，不能出现循环导入。

二、功能要求

1. 选择图片：点击按钮选择多张图片（支持 JPG、PNG 格式），选完后在界面上显示文件路径和原始大小。
2. 压缩质量：一个滑动条（50-100），默认 80，实时显示当前数值。
3. 输出文件夹：一个选择文件夹按钮，压缩后的图片保存到这里。
4. 压缩按钮：点击后遍历列表中的每张图片进行压缩，显示进度条。
5. 压缩逻辑：
   · JPG 格式：用 PIL 的 save 方法设置 quality 参数
   · PNG 格式：设置 optimize=True 和 compress_level
   · 压缩后文件名保持原名，不要覆盖原图
6. 结果提示：压缩完成后弹窗提示'完成 X 张，失败 Y 张'。
7. 压缩后的图片可以导出到指定文件夹。

三、界面布局（在 app.py 中实现）

· 窗口大小 600x500，使用 CustomTkinter 深色主题
· 顶部：'选择图片'按钮，旁边显示已选图片数量
· 中间：列表框显示已选图片路径（可滚动）
· 中间偏下：'压缩质量'标签 + 滑动条 + 数值
· 下方：'输出文件夹'按钮 + 显示选中的文件夹路径
· 底部：'开始压缩'按钮 + 进度条

四、技术要求

· 用 customtkinter 做界面
· 用 PIL 处理图片
· 压缩过程不要卡死界面（使用 threading 或 update_idletasks）
· 必须处理异常（损坏图片、格式不支持等），异常信息要友好提示给用户

五、输出要求

请分别输出每个文件的完整代码，开头用注释说明文件名。例如：

```python
# ========== main.py ==========
import ...
```

代码要有必要注释，结构清晰。"


以下是迭代后的图片批量压缩工具提示词，在原版本基础上增加了需求迭代的说明方式：

---

"帮我用 Python 和 CustomTkinter 开发一个图片批量压缩工具。请按照迭代的方式逐步完善，每个版本增加新功能，而不是一次性全部实现。

整体要求

· 代码必须拆分到多个文件，方便维护
· 使用 CustomTkinter 深色主题
· 使用 PIL 处理图片
· 压缩过程不卡界面（用 threading）

文件结构（保持不变）

```
image_compressor/
├── main.py           # 程序入口
├── app.py            # 主窗口类
├── compressor.py     # 压缩逻辑
├── file_selector.py  # 文件选择
├── task_manager.py   # 任务管理
└── utils.py          # 辅助函数
```





请基于现有 基础版代码，增加以下功能，作为 V1.1 版本：

新增功能要求

1. 多选图片：支持同时选择多张图片，列表中显示每张图片的完整路径和原始文件大小
2. 清除列表：添加一个"清除列表"按钮，点击后清空当前所有已选图片，方便重新选择
3. 移除选中：添加"移除选中"功能，支持从列表中移除勾选的图片（通过复选框或多选模式）
4. 拖拽添加：支持将图片文件或文件夹直接拖拽到程序窗口，自动解析并添加到图片列表
5. 窗口可缩放：窗口外边界支持鼠标拖拽调整大小，窗口内的组件（列表、按钮、进度条等）应能自适应缩放或提供滚动条，避免界面错乱

界面布局要求

· 请合理规划新按钮的摆放位置，保持界面整洁
· 列表需支持多选（按住 Ctrl 或 Shift 进行多选）或每行带复选框
· 拖拽功能需给出视觉反馈（如拖拽时改变边框颜色或鼠标样式）

输出要求

· 保持原有的多文件结构（main.py、app.py、compressor.py、file_selector.py、task_manager.py、utils.py）
· 仅需输出本次改动的文件（主要是 app.py 和 file_selector.py），其他未修改的文件可以说明"保持不变"
· 代码需添加必要注释，标明新增或修改的部分

